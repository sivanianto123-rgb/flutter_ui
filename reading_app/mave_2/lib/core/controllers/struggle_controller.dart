// StruggleController — superseded by SoundScoreService + soundAccuracyProvider.
// Adaptive difficulty is no longer used; accuracy-based unlock is handled by
// SoundNode.statusFor(accuracyMap).  File retained for git history only.
library;
